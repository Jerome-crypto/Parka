services:
  # 1. Backend Express Web Service
  - type: web
    name: parka-server
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install --include=dev && npx prisma generate && npx prisma db push --accept-data-loss && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: parka-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://parka.onrender.com

  # 2. Frontend Vite React Static Site
  - type: web
    name: parka
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://parka-server.onrender.com
      - key: VITE_SOCKET_URL
        value: https://parka-server.onrender.com
    routes:
      # Redirect/Rewrite rule for SPA React Router
      - type: rewrite
        source: /*
        destination: /index.html

# 3. PostgreSQL Database Instance
databases:
  - name: parka-db
    plan: free
